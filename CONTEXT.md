# 离线项目导航

描述离线开发环境中，用户如何指定项目导航的文件范围。

## Language

**本地文件清单（filelist）**：
用户指定的若干扫描目录中，符合指定后缀的文件路径集合；路径相对于项目根目录表示。该清单属于本机生成物，不纳入 Git 版本管理。
_Avoid_：语义索引、编译数据库（两者均包含文件路径以外的信息）

**清单项目根**：
一份本地文件清单所属项目的路径基准。项目外的共享 SDK 也可以属于这份清单的导航范围。
_Avoid_：扫描目录、SDK 根目录

**清单导航范围**：
一份本地文件清单明确列出的文件集合，包含用户选定的项目文件和外部 SDK 文件。
_Avoid_：编译单元集合、LSP 工作区（不保证与清单范围一致）

**文本候选**：
在清单导航范围内，与光标下符号同名的文本位置，可能是定义、声明、调用、注释或条件编译中的内容。
_Avoid_：语义定义、语义引用
