# 文件清单用于文件定位和文本回退，保留 LSP 的语义结果

离线项目希望通过本地文件清单覆盖显式选择的源码和共享 SDK，减少头文件定位对构建环境的依赖。选择让 include 优先使用清单，符号定义和引用优先使用 LSP，失败时在清单范围内显示明确标注的文本候选；LSP 结果不受清单过滤，因为路径集合不能表达编译参数、宏条件和符号身份。由此同名文本不会被当作可靠的语义定义或全部真实引用，已有编译数据库支持仍有作用。
